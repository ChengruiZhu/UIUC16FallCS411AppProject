
def pinPoint(string:city/country):
  return {[long, lnog] 经纬度}  +  {movies with meta data in json format}  in json format
  
  就是经纬度作为json file里的第一个pair, following pairs 就是普通的movie+meta data.
  
  
  
  
def getMovies([long, long] 经纬度, [int, int] date_range, string genre, [int, int] rating_range):
  return {movies with meta data in json format}
  
这个是return单纯的movie+meta data, json format




def getRecommendation(string targetMovieName):
  return {recommended movies with meta data in json format}
  
  这个format和上面一样都是json
