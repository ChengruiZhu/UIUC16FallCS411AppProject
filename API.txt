def getMovies([long, long, long, long] 经纬度bound, [int, int] date_range, string genre, [int, int] rating_range):
  return {movies with meta data(including x,y) in json format}
  
这个是return单纯的movie+meta data, json format





def getRecommendation(string targetMovieName):
  return {recommended movies with meta data in json format}
  
  这个format和上面一样都是json
